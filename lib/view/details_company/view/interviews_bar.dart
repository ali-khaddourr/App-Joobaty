import 'package:flutter/material.dart';

class InterviewsBar extends StatelessWidget {
  const InterviewsBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}